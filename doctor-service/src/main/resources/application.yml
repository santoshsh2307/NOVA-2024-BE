server:
  port: 8088
  
spring:
  application:
    name: DOCTOR-SERVICE
    
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost